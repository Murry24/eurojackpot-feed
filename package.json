{
  "name": "eurojackpot-feed",
  "type": "module",
  "private": true,
  "scripts": {
    "build:latest": "node fetch.js",
    "build:history": "node tools/build_history.js",
    "merge:history": "node tools/merge_history.js",
    "build": "npm run build:latest && npm run build:history && npm run merge:history"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  }
}
